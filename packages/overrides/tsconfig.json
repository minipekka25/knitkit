{
  // Typecheck + editor config (build is done by tsup, not tsc). See runtime/tsconfig.json.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src", "test"],
  "exclude": ["dist", "node_modules"]
}
