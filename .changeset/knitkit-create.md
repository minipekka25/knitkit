---
"@knitkit/create": minor
---

New package: `npm create @knitkit my-app` scaffolds a runnable host + remote starter (shared
singleton, zero build step) so you can see knitkit working in one command.
